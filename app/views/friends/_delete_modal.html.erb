<button type="button" class="btn btn-danger btn-block mt-3" data-toggle="modal" data-target="#modalDelete">
  Delete friend
</button>
<div class="modal fade" id="modalDelete" tabindex="-1" role="dialog" aria-labelledby="modalDeleteLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="modalDeleteLabel">Confirm</h5>
      </div>
      <div class="modal-body">
        Do you really want to delete relation with <strong><%= @friend.email %></strong> and all shares?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <%= button_to "Delete", friend_path(@friend.id), method: :delete, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>